<%= form_for(baseline) do |f| %>
  <% if baseline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(baseline.errors.count, "error") %> prohibited this baseline from being saved:</h2>

      <ul>
      <% baseline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.text_field :product_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
